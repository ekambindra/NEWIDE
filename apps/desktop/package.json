{
  "name": "@ide/desktop",
  "version": "0.1.0",
  "description": "Atlas Meridian desktop AI IDE",
  "author": "Atlas Meridian",
  "private": true,
  "type": "module",
  "main": "dist/main/main.js",
  "scripts": {
    "dev": "npm run dev:deps && npm run dev:port-check && concurrently -k \"npm:dev:main\" \"npm:dev:renderer\" \"npm:dev:electron\"",
    "dev:deps": "npm run build --workspace @ide/shared && npm run build --workspace @ide/policy-engine && npm run build --workspace @ide/indexer && npm run build --workspace @ide/agent-runtime && npm run build --workspace @ide/benchmark",
    "dev:port-check": "node -e \"const net=require('node:net');const server=net.createServer();server.once('error',()=>{console.error('Port 5173 is already in use. Stop existing Atlas Meridian dev sessions and retry.');process.exit(1);});server.once('listening',()=>server.close(()=>process.exit(0)));server.listen(5173,'127.0.0.1');\"",
    "dev:main": "tsc -p tsconfig.main.json --watch --preserveWatchOutput",
    "dev:renderer": "vite --host 127.0.0.1 --port 5173 --strictPort",
    "dev:electron": "wait-on dist/main/main.js tcp:5173 && cross-env VITE_DEV_SERVER_URL=http://localhost:5173 electron .",
    "build": "npm run build:main && npm run build:renderer",
    "build:main": "tsc -p tsconfig.main.json",
    "build:renderer": "vite build",
    "dist:mac": "npm run build && electron-builder --config electron-builder.yml --mac dmg zip --publish never",
    "dist:win": "npm run build && electron-builder --config electron-builder.yml --win msi nsis --publish never",
    "release:desktop:mac": "npm run build && electron-builder --config electron-builder.yml --mac dmg zip --publish always",
    "release:desktop:win": "npm run build && electron-builder --config electron-builder.yml --win msi nsis --publish always",
    "release:desktop:all": "npm run build && electron-builder --config electron-builder.yml --mac dmg zip --win msi nsis --publish always",
    "release:stable": "cross-env ATLAS_UPDATE_CHANNEL=stable npm run release:desktop:all",
    "release:beta": "cross-env ATLAS_UPDATE_CHANNEL=beta npm run release:desktop:all",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.main.json --noEmit && tsc -p tsconfig.renderer.json --noEmit"
  },
  "dependencies": {
    "@ide/agent-runtime": "0.1.0",
    "@ide/benchmark": "0.1.0",
    "@ide/indexer": "0.1.0",
    "electron-updater": "^6.3.9",
    "ignore": "^7.0.5",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@electron/notarize": "^2.5.0",
    "electron": "35.0.3",
    "@types/electron": "^1.6.12",
    "@types/react": "^19.0.12",
    "@types/react-dom": "^19.0.4",
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "electron-builder": "^24.13.3",
    "vite": "^6.2.2",
    "wait-on": "^8.0.2"
  }
}
