# Security

## Secure-by-default controls

- Balanced policy mode blocks dangerous operations unless approved
- Sensitive path classes: `infra`, `security`, `auth`
- Secrets detection and redaction in logs/audit events

## Data handling

- Telemetry opt-in only
- Local-only mode supported
- Managed cloud mode requires TLS and signed request envelopes
